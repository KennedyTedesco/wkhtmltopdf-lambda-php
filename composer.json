{
    "require": {
        "ext-json": "*",
        "bref/bref": "^1.2",
        "knplabs/knp-snappy": "^1.2"
    },
    "scripts": {
        "deploy": [
            "composer install --optimize-autoloader --no-dev",
            "serverless deploy"
        ]
    }
}
